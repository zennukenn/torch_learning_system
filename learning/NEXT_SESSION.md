# Next learning session — generated hot context

> Generated by `scripts/build_next_session.py`; edit its source files, not this file.

## Resume contract

- Status: Foundation 0.2 completed; MiniTorch project-assessment transition accepted on 2026-09-13
- Current phase: Phase 0 / MiniTorch M0a — fast native-import bootstrap
- Source root: `sources/pytorch` (independent Git repository; ignored by the learning-system repository)
- Commit: `cf30153c4c131c8164ee7798e5022d810682e2cb` (`v2.13.0`, detached HEAD)
- MiniTorch root: `mini-torch` (independent Git repository initialized on 2026-09-15; no commits; current README/ADR/ignore files are mentor drafts and are not learner evidence)
- Last session: 2026-09-02 — Foundation 0.2; independently explained the Tensor minimal model and Flatten invariants, with H1/H2 correction for direct callee and evidence boundary
- Next action: resume M0a with one project microcycle: teach Git tracked/generated boundaries, then have the learner inspect and materially rewrite or replace the mentor-drafted README/ADR/ignore policy before any mastery claim
- Active blockers: the 2026-09-16 read-only preflight finds C++/Python headers, pinned vendored pybind11, nvcc and GPU visibility, but CMake is the sole missing M0a-required capability; Ninja and GDB remain unavailable optional/later tools; WSL RAM is 7.6 GiB; the isolated direct-compiler fixture passes but does not satisfy the learner-owned CMake gate; real third-party adapter/hardware tests must run privately
- Latest completed notebook: `learning/notebook/sessions/2026-09-02-foundation-02-tensor-minimal-model.md`

## Current course slice

- Course: `C00` — Architecture and native bootstrap
- Milestone/release: `M0a` / `A`
- Knowledge for this slice: Inference panorama repository boundaries Git Python import compile link CMake and a basic pybind11 module
- Immediate learner action: Create the independent repository then author its architecture map README ignore policy ADR CMake target and first native module
- Gate evidence: Git boundary checks plus a clean native import and learner explanation prove the source-to-extension path
- Relevant mastery:
  - `PYTORCH-BASICS`: learning; explain=3, locate=0, trace=0, debug=0, modify=0, transfer=0, retain=0
  - `ARCH-MAP`: learning; explain=2, locate=1, trace=0, debug=0, modify=0, transfer=0, retain=0
  - `ENV-BUILD`: unassessed; explain=0, locate=0, trace=0, debug=0, modify=0, transfer=0, retain=0
  - `CPP-CORE`: learning; explain=0, locate=0, trace=0, debug=0, modify=0, transfer=0, retain=0
  - `PY-DATAMODEL`: learning; explain=1, locate=0, trace=0, debug=0, modify=0, transfer=0, retain=0

## Calibrated learner baseline

- `Tensor`、神经网络 `forward`、Autograd/`backward`：听说过，需要从直觉、用途和简单例子开始。
- CPU 与 GPU：自评能解释；后续仍用小任务验证具体边界。
- Python `class` / `import`：听说过，需要从 reduced example 系统建立执行模型。
- C++ pointer / reference：听说过；compile/link：不了解。
- 更早的 Python 5/10、C++ 3/10、深度学习 5/10、系统 3/10 是宽泛自评，不能作为进入深层源码的 prerequisite evidence。
- 数学学过线性代数、微积分、概率统计和数值计算，按实际模型/算子语境激活。

## Eligible project-grounded reviews

- CPP-CORE.explain — 在 M0a 解释自己的 compile/link path；`const Tensor&` 在 M1 Tensor handle 中复测。 (hint ceiling H0)
- PYTORCH-BASICS.explain — 在 M0a 目录架构图中放置 Tensor、operator、Module/forward、device、training 和 inference。 (hint ceiling H0)
- ARCH-MAP.explain — 用目标 CNN inference 说明 MiniTorch 各里程碑最终如何连接 input 到 output。 (hint ceiling H0)
- ARCH-MAP.locate — 在 M0a 目录 mapping 中定位真实 `Linear.forward` 与计划中的 MiniTorch frontend 位置。 (hint ceiling H0)
- PY-DATAMODEL.explain — 在 M0a `minitorch` import/binding code 中解释 method、`self` 和 type annotation。 (hint ceiling H0)
- TENSOR-MODEL.explain — 在首个 MiniTorch runtime test 中区分 MiniTorch、pinned PyTorch source 和 official binary evidence。 (hint ceiling H0)

## Cold-context routing

- Ordinary session: read only the current milestone section and the relevant teaching reference.
- Milestone/scope change: read the accepted specs, full roadmap and coverage audit.
- PrivateUse/compiler work: additionally read the matching backend/inference specification.
- Assessment write: use one reviewed session manifest; load schema details only then.
